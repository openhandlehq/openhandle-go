name: Remove accidental Go v1.0.1 release

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  remove:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Create SDK release token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.SDK_SYNC_APP_ID }}
          private-key: ${{ secrets.SDK_SYNC_APP_PRIVATE_KEY }}
          owner: openhandlehq
          repositories: openhandle-go
          permission-contents: write

      - name: Remove accidental release and tag
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          REPOSITORY: ${{ github.repository }}
        run: |
          if gh release view v1.0.1 --repo "$REPOSITORY" >/dev/null 2>&1; then
            gh release delete v1.0.1 --repo "$REPOSITORY" --cleanup-tag --yes
          fi

          if gh release view v1.0.1 --repo "$REPOSITORY" >/dev/null 2>&1; then
            echo 'The accidental v1.0.1 release still exists.' >&2
            exit 1
          fi

          if gh api "repos/$REPOSITORY/git/ref/tags/v1.0.1" >/dev/null 2>&1; then
            echo 'The accidental v1.0.1 tag still exists.' >&2
            exit 1
          fi

# Pinned OpenAPI contract

`openhandle.json` is the exact public API contract used to generate this SDK.
It is exported from the API application after production deployments.
Regeneration must be deterministic and produce no diff in CI.
